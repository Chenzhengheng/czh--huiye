# AI Diary · MVP 产品定义

> 状态：已确认，可用于指导 MVP 实现  
> 更新日期：2026-07-12

## 1. 产品定位

AI Diary 不是“让 AI 帮用户写日记”，而是一个轻量的**思考连续性系统**：

> 用户只负责记录，不必整理；过去尚未结束的思考，会在未来合适的时刻重新接上。

产品总纲：

> **轻量，先有用，后回味。**

- **先有用**：今天写下就能保存、整理、搜索和导出，不要求用户先维护复杂系统。
- **后回味**：旧疑问在新经历中得到回应，用户能够看见自己的思考如何随条件、场景与真实反馈发生变化。

## 2. 核心问题

用户并不缺少记录工具。真正的问题是：记录虽然发生了，思考却没有继续发生。

- 阅读批注散落在书籍和手写笔记中，后来遇到答案时无法找回当初的疑问与推理。
- 工作思考散落在飞书、文件夹和临时文档里；如果不花时间起标题、分类和整理，内容很快沉底。
- 整理本身不是思考，却持续消耗记录意愿。
- 大量记录停留在当下的表达，没有在未来被重新使用和内化。

## 3. 第一目标用户

经常产生并记录想法，但记录散落在阅读批注、工作工具和临时文档中；不愿维护复杂知识库，又希望自己的思考能够被找回、延续和内化的人。

这类用户已经有记录行为，阻碍他们的不是“不会写”，而是：

- 捕获后的整理成本；
- 长期找回的困难；
- 记录复用率太低带来的挫败感。

## 4. 核心使用场景

### 4.1 阅读中的手写思考

用户用平板阅读，通过手写记录某个段落带来的疑惑、推理或复现。后来读到答案或遇到相似问题时，系统帮助用户找回当时的内容、上下文和思考原因。

### 4.2 工作中的即时思考

用户对某件事产生想法时直接记录，不需要先新建文件夹、起标题或选择分类。系统在用户授权后补齐结构，并让内容在未来相关对话中重新出现。

## 5. 价值闭环

### 捕获

文字、粘贴、导入与手写 OCR 都可以快速进入。用户不必先决定标题、格式和分类。

### 回响

当用户主动回忆、反思自己，或借过去的经验推演未来时，AI 找回相关原始记录或关联摘要。

### 沉淀

用户对照过去的疑问、当时的判断与后来的事实，补充或修正理解。系统保存思考的演变，而不只是生成一次性总结。

## 6. 核心差异：思考回环

首版最重要的长期价值是发现**未闭合的问题**，并把今天的新经验接回去：

```text
过去的疑问
    ↓
当时的推理与条件
    ↓
后来发生的事实或获得的反馈
    ↓
今天的新理解
    ↓
仍未解决的部分
```

“未闭合问题”会自然延伸为“思想变化”：过去的疑问可能只有一个问号，也可能包含受限于旧条件、旧场景和缺少真实反馈的判断。AI 的作用是带回证据、建立连接并轻轻询问，而不是替用户宣布结论。

## 7. AI 的双通道响应

### 7.1 用户主动响应（约 90%）

用户写下若干笔记并进入内容池，随后与 AI 对话。用户可能在回忆过去、反思自己，或依据过去的教训和思考推演未来。

产品机制：

1. 对话时后台执行轻量检索，不要求用户明确说“帮我回忆”。
2. AI 判断召回内容是否足够相关、是否值得展示。
3. 召回对象可以是用户原文，也可以是 AI 整理的跨记录关联摘要。
4. 展示时区分原始记录、关联摘要与当前推断，并允许回到原文。

### 7.2 用户被动响应（约 10%）

AI 在后台整理时，如果发现高价值且不显然的行为变化、巧思、未闭合问题或跨场景关联，只给出一个低打扰邀请：

> 你之前留下的一个问题，似乎在最近的记录里有了新答案。要一起看看吗？

“10%”是一种体验原则，而不是机械比例：惊喜必须稀缺。用户忽略后不追问，不连续打扰。

主动提示必须满足：

- 有新意，而不是泛泛总结；
- 有具体记录作为证据；
- 对用户当下可能有意义；
- 展开后确实有内容；
- 不主动进行敏感人格判断或心理诊断。

## 8. AI 的性格与边界

- 接住情绪，轻轻询问。
- 提供证据，不替用户定义自己。
- 可以检索，但前台必须克制；检索到不等于一定展示。
- 不把用户的表达润色成标准作文。
- 不改变观点，不擅自补写结论。
- 关联摘要可以查看、修正和删除。

## 9. 写完后的首版流程

```text
用户写完
   ↓
原文立即保存
   ├─ 关闭 AI 整理：直接退出
   └─ 开启 AI 整理：后台轻量处理
                         ↓
                  “AI 正在整理你的日记…”
                         ↓
                  用户确认整理结果
```

整理完成后提供：

- 接受整理；
- 稍作修改；
- 保留原文。

原始版本永远保留，AI 不直接覆盖。

## 10. 两个独立的 AI 开关

两个开关默认开启，但首次使用时必须明确说明并由用户确认。确认后记住默认偏好，同时允许单篇关闭。

### AI 帮我整理

控制 AI 是否处理和改写当前记录。

### 允许 AI 关联

控制当前记录是否进入未来的检索、对话召回、主动发现和跨日记总结。

| AI 整理 | AI 关联 | 产品行为 |
|---|---|---|
| 开 | 开 | 整理本文，并用于未来召回 |
| 开 | 关 | 只整理本文，之后不参与关联 |
| 关 | 开 | 保留原文，但原文可以被召回 |
| 关 | 关 | 原样保存，作为完全私密记录 |

关闭“AI 关联”后，系统不仅不能召回原文，也不能继续保留或使用由本文生成的关联摘要。

## 11. AI 整理的首版边界

AI 整理只做：

- 修正 OCR 与明显错字；
- 调整段落和 Markdown 格式；
- 生成标题与少量标签；
- 提取疑问、当前想法和待验证内容。

用户认可一次整理后，可以选择“记住此类记录的整理偏好”。该偏好只用于相似类型的记录，不全局机械套用。

## 12. MVP 功能范围

### 必须完成

- 轻量文本记录；
- 原文立即保存；
- “AI 整理”与“AI 关联”两个独立开关；
- 模拟 AI 整理状态与整理稿确认；
- 日记池及基本检索；
- AI 对话中的相关记录召回；
- 一条未闭合问题的克制提示；
- 原文、整理稿和关联依据可追溯；
- Markdown 导出。

### 暂不进入首版

- 完整手写编辑器；
- 真实 OCR 服务；
- 飞书与微信读书的正式连接器；
- 复杂知识图谱；
- 自动心理分析；
- 多人协作；
- 跨设备云同步和正式账号体系。

首版可使用示例数据和本地存储证明产品闭环，后续再接入真实模型、OCR、向量检索与云端数据层。

## 13. MVP 成功标准

首版不以“记录了多少篇”为唯一指标，重点验证：

1. 用户能否不经过标题和分类快速完成记录；
2. 用户是否理解并信任两个 AI 开关；
3. AI 整理是否省事而不篡改表达；
4. 旧问题被召回时，用户是否认为关联准确且有价值；
5. 用户是否愿意因为“未来可能重新接上”而继续留下记录。

北极星指标候选：

> 被用户认可的“思考回环”数量，而不是日记总篇数。

