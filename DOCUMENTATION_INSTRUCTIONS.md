# 回页｜文档维护说明

> 生效日期：2026-07-24

项目只维护三类活跃文档。它们回答不同问题，不应互相复制成三份完整说明。

[DOCUMENTATION_INDEX.md](DOCUMENTATION_INDEX.md) 是所有文档的统一入口。新增、归档或移动文档时，必须同步更新索引。

## 1. 产品共识：`README.md`

回答“回页是什么、为什么这样做、哪些原则不能轻易改变”。

应包含：

- 一句话定位与核心假设；
- 用户问题、核心体验与产品原则；
- AI 的角色和明确非目标；
- 回响机制、记忆模型、数据归属等稳定取舍。

不应包含：

- 具体代码字段和实现细节；
- 短期任务清单；
- 已经失效的功能操作说明。

当产品方向发生变化时，先更新这里，再同步另外两类文档。产品判断发生冲突时，以此文档为准。

## 2. 阶段规划：`ROADMAP.md`

回答“当前验证什么、接下来先做什么、什么暂时不做”。

应包含：

- 当前阶段与北极星指标；
- 已完成、正在验证和下一阶段；
- P0 / P1 优先级；
- 用于决定是否继续投入的真实反馈信号。

不应包含：

- 重复完整产品定义；
- 详细数据结构和接口说明；
- 已完成却仍写成待办的事项。

功能上线、取消或优先级变化时更新这里。它可以快速变化，但不得违反 `README.md` 的产品原则。

## 3. 技术实现：`TECHNICAL_GUIDE.md`

回答“当前代码实际上如何运行、数据在哪里、接口和边界是什么”。

应包含：

- 当前技术栈与运行架构；
- API、数据模型和本地存储方式；
- 导入导出、部署和开发所需事实；
- 遗留实现、兼容字段和已知技术边界。

不应包含：

- 尚未实现却写成已实现的能力；
- 用代码遗留反向定义产品方向；
- 大段产品论证或路线优先级。

实现变化时同步更新这里。如果代码与产品规格暂时不一致，应明确标注“遗留、兼容或待清理”，不能把遗留实现写成现行产品能力。

## 冲突处理顺序

```text
产品方向冲突：README.md
        ↓
阶段优先级冲突：ROADMAP.md
        ↓
实现事实冲突：TECHNICAL_GUIDE.md，并以当前代码核验
```

README 决定产品要去哪里，路线图决定现在先走哪一步，技术指南说明此刻机器实际上怎样运转。

## 有截止日期的交付计划

交付计划放在 `docs/plans/`，使用 YAML front matter 标明 `document_type`、状态、时间范围、版本目标和更新时间，以区别于三类核心文档。

交付计划负责记录：

- 每日任务、负责人和完成状态；
- 明确的 V1 / V2 验收标准；
- good case、bad case、风险和范围调整；
- 当期作品集、视频和简历等交付物。

交付计划不能修改产品原则。计划结束后，先把仍然有效的结论同步回核心文档，再将计划状态改为 `completed` 并保留归档。

## 历史文档

退出主线但仍有追溯价值的资料放入 `docs/legacy/`，并在开头标明：

- 它为何过时；
- 当前由什么决策替代；
- 保留它是为了维护、迁移还是历史追溯。

历史文档不列入“关键文档”，也不能作为新功能实现依据。确认代码、数据和回归测试都不再依赖后，才考虑删除。

## 每次功能变化后的检查

1. 产品原则是否改变？若是，更新 `README.md`。
2. 完成状态或优先级是否改变？若是，更新 `ROADMAP.md`。
3. 接口、字段、存储或真实能力是否改变？若是，更新 `TECHNICAL_GUIDE.md`。
4. 是否产生退出主线的说明？若是，移入 `docs/legacy/` 并加历史标记。
5. 是否新增、归档或移动文档？若是，更新 `DOCUMENTATION_INDEX.md`。
6. 搜索“后续、待完成、当前、可选”等表述，确认没有与现实相反的陈述。
