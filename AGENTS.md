# 回页项目协作规则

## 强制维护流程

对本项目进行任何维护（包括代码、测试、配置、数据结构、界面和文档修改）时，必须同时使用以下技能：

1. `grill-me`
2. `grill-with-docs`

执行顺序如下：

1. 先运行 `grill-me`，按照 `grilling` 技能逐题访谈；一次只问一个需要用户决定的问题，并为每个问题给出推荐答案。
2. 能通过项目文件、代码、测试或工具查明的事实必须自行核查，不得把事实性问题交给用户。
3. 再运行 `grill-with-docs`，结合 `domain-modeling` 技能和 `docs/` 中的项目文档继续对齐，并用具体场景、边界情况和代码现状检验结论。
4. 在用户明确确认“已充分对齐”之前，不得开始实施改动。
5. 达成术语共识时立即更新 `docs/04_CONTEXT.md`。该文件是本项目现有且唯一的领域术语表，等同于 `domain-modeling` 技能所称的 `CONTEXT.md`；不要另建根目录 `CONTEXT.md`。
6. 只有当决策同时满足“难以逆转”“缺少背景会令人意外”“源于真实取舍”三个条件时，才在 `docs/adr/` 中创建 ADR。
7. 根据 `docs/01_DOCUMENTATION_INDEX.md` 规定的文档职责，同步维护受到改动影响的文档；不得把目标状态描述成已经实现的现状。

这套流程适用于所有后续维护任务，不因改动看似简单而跳过；如果用户明确要求变更本规则，应先重新进行上述对齐流程。
