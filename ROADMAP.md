# 回页｜规划

> 目标：在 2026 年 8 月中旬前验证“恰当带回过去的思考，能否促成新的思考”。

## 北极星指标

不是功能数量，而是一次回响后是否发生以下任一行为：

1. 打开旧记录；
2. 沿着旧记录继续写；
3. 用户认为这次确实相关、有用；
4. 用户因相信它以后会回来，而更愿意留下真实思考。

## 当前阶段：回响 0.1

### 已具备

- 写作、草稿、编辑、删除和 Markdown；
- 每篇记录只保留一份用户确认的「我的思考」；
- 每篇记录可单独决定是否允许 AI 关联；
- 保存后静默建立候选关联；
- 回响页一次展示一篇带原句和理由的旧记录；
- 用户可打开、继续写或判定无关；
- Markdown 多篇导出；
- 完整 JSON 备份与导入，可恢复图片、思考线、回响和回响检查记录。

### 要用真实日记验证

- 关联是否真相关，还是只是关键词相近；
- 一次 1 篇是否舒服；
- 什么时间错位最自然；
- “继续写”是否真的发生；
- 哪些理由令人可信，哪些令人牵强。

### 当前验证方法

- 暂不训练回页专属模型：当前既没有必要，也没有足够的高质量反馈数据；
- 召回判断可以先由通用模型、人工或 CLI 流程完成，先验证“带回是否有用”，再决定自动化方式；
- CLI 是内部研究工具，不替代“随时写下、不需要管理”的用户体验；
- 每次实验都保留候选、带回理由和用户反馈，确认这些信号确实能改善后续召回，才把闭环称为飞轮。

### 15 篇检查点

当真实思考积累达到 15 篇时，再继续设计并验证回响 SOP：

1. 导出一份完整 JSON 备份；
2. 建立人工验证索引，记录每篇思考的 ID、时间、核心判断、理由或条件、未完成问题；
3. 用最新思考作为 T2，从索引中寻找可能有用的 T1；
4. 保存“应该回响 / 应该沉默”的判断、原句证据、变化量和继续问题；
5. 观察是否产生更新后的 T3，再决定是否自动化召回。

这份索引是研究工具，不是知识图谱、向量数据库或需要用户维护的分类系统。

### 记录的反馈信号

- `opened`：打开旧记录；
- `continued`：沿着它继续写；
- `irrelevant`：这次无关；
- 无操作：不作为正向信号。

## 下一阶段：主动对话召回

在“和 AI 聊聊”中，用户带着当前问题发起请求：

- 只检索允许关联的原文；
- 最多引用少量记录；
- 明确给出来源与关联理由；
- 没有证据就说没有找到；
- AI 把证据交还给用户，不替用户作答。

这与回响共用关联判断和使用反馈，但由用户主动触发。

## 之后才考虑

1. 思考线的可视化与回看；
2. 已被带回过的记忆如何冷却、何时可再次出现；
3. 多篇召回的阈值与层级；
4. 真正的持久化、跨设备与账户；
5. 向量检索与更大规模日记池；
6. 稀少且可关闭的主动 hint。

## 每周决策问题

- 回响是否宁可空白，也没有牵强？
- 用户是否愿意沿着旧思考继续写？
- 旧记录的原句与时间，是否足以让用户重新进入？
- 回响理由是否可核对，并且足够轻，不替用户下结论？

若这些问题无法用真实体验回答，就不增加新功能。

## 当前取舍：暂停 AI 整理，验证回响

当前不再继续投入 AI 整理。每篇记录只保留一份用户确认的「我的思考」，把时间留给保存可靠性、回响验证与主动对话召回。

### 已完成的基础

- Markdown 多篇导出；
- 完整 JSON 备份；
- 同格式 JSON 导入；
- 明确提示浏览器本地数据不随网址同步。

### 当前 P0

1. 用真实日记验证回响是否有用；
2. 将“和 AI 聊聊”从静态演示改为真实、可解释的主动召回；
3. 修复日记池筛选按钮，使其真正筛选；
4. 移除硬编码日期，使用真实当前日期；
5. 验证导入后的回响与图片恢复完整；
6. 清理确认不再需要的 AI 整理代码、旧 Prompt 和样例状态。
