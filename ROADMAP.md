# 回页 · 产品与技术路线图

> 更新日期：2026-07-12  
> 目标：把当前交互原型推进为可长期使用、可解释、尊重隐私的 AI 日记。

## 1. 到目前为止，我们确认了什么

### 产品不是 AI 聊天框加日记

回页是一个**思考连续性系统**：用户只负责记录，不必维护复杂知识库；过去尚未结束的思考，会在未来合适的时刻重新接上。

产品总纲：

> **轻量，先有用，后回味。**

- 先有用：快速记录、可靠保存、编辑、搜索、导入和导出。
- 后回味：旧疑问在新经历中得到回应，用户看到自己的思考如何变化。

### 第一目标用户

经常产生并记录想法，但内容散落在阅读批注、飞书和临时文档中；不愿维护复杂知识库，又希望自己的思考能够被找回、延续和内化的人。

### 首要长期价值

先发现**未闭合的问题**，再自然延伸到**思想变化**：

```text
过去的疑问
  → 当时的推理和条件
  → 后来发生的事实与反馈
  → 今天的新理解
  → 仍未解决的部分
```

### AI 的出现比例

- 约 90%：用户主动对话，系统在后台召回相关记录，相关性足够高才展示。
- 约 10%：AI 发现高价值且不显然的回环，只发出低打扰邀请。

惊喜必须稀缺。主动提醒只能建立在“主动召回已经足够准确”之后。

### 已确定的交互原则

- 写作是视觉中心，工具退后。
- 输入区是一张随思考展开的纸，不是固定表单。
- 少于 15 行时，正文逐渐展开，并保留约 3 行呼吸空间。
- 达到 15 行后高度稳定，标题退出视野，当前输入位置下方保留安全距离。
- 原文永远保留；AI 产物不能静默覆盖原文。
- “AI 整理”和“AI 关联”是两个独立权限，首次明确授权后默认开启。

## 2. 当前状态

当前版本已经证明了产品感觉与主要交互方向，但仍属于交互 MVP。

已具备：

- 轻量写作与弹性编辑器；
- 本地日记池和搜索；
- 文本粘贴与图片附件；
- 两个独立 AI 开关；
- 模拟 AI 整理和整理稿确认；
- 标签增删；
- 日记查看、编辑、删除；
- 单篇、多篇 Markdown 导出；
- 模拟思考回环和对话召回；
- 浏览器本地保存。

尚未具备：

- 正式账号和跨设备数据；
- 云端数据库与附件存储；
- 真实模型调用；
- 真实 OCR；
- 真实检索、RAG 和记忆；
- 可度量的召回质量；
- 完整隐私删除链路。

## 3. 可行性结论

### 明确可行

- 可靠的日记增删改查、版本和 Markdown 导入导出；
- AI 标题、格式整理、标签和结构提取；
- OCR 图片转文字；
- 关键词与语义混合检索；
- 对话中引用原始记录；
- 用户确认后形成可修改记忆。

### 主要难点

难点不是“调用一个大模型”，而是长期可靠性：

1. **召回准确**：相关内容找得准，且不会因为语义相似而牵强关联。
2. **记忆可信**：AI 推断不能自动变成“关于用户的事实”。
3. **权限真实**：关闭关联后，原文、向量、摘要和记忆引用都必须同步失效。
4. **可解释**：用户能看到引用日期、来源、原文及关联理由。
5. **低打扰**：被动提示需要阈值、冷却时间和负反馈机制。
6. **可评估**：需要持续记录“相关 / 不相关 / 有启发 / 冒犯”等真实反馈。

### 总体判断

个人开发完全可行。一个真实可用的主动召回 Beta，按业余时间预计需要约 **8–12 周**；包含可靠记忆和克制主动提示的版本，预计 **12–18 周**。它适合持续数年的个人迭代，不适合一次性堆完所有功能。

## 4. 推荐技术架构

### 4.1 客户端与应用层

- TypeScript、React、Next/Vinext：延续现有代码，不重写前端。
- Cloudflare Workers / Sites：承载应用和服务端接口。
- 响应式 Web：先覆盖电脑和平板，再决定是否做原生 App。

### 4.2 数据层

- **D1**：用户、日记、版本、标签、权限、对话、记忆和反馈等结构化数据。
- **R2**：原始图片、OCR 文件和未来的大附件。
- **Vectorize**：允许关联内容的向量索引。
- **D1 FTS5**：关键词全文检索。

选择这一组合的原因：当前站点已经运行在 Cloudflare 体系中，D1 使用 SQLite 查询模型，支持 FTS5；Vectorize 可以通过元数据关联回 D1/R2 中的原始对象。这样不用过早维护独立服务器。

### 4.3 AI 层

- 使用 OpenAI Responses API 作为生成入口，但在代码中保留模型适配层，不让产品绑定单一模型名称。
- 低成本模型：标题、标签、格式整理、候选问题提取。
- 更强模型：跨时间推理、思考回环和复杂对话。
- Embeddings：把允许关联的文本转换为向量，用于语义召回。
- Structured Outputs：要求整理、标签、记忆候选等返回稳定结构，而不是自由文本。

模型是可替换零件，产品真正的资产是：用户数据、权限、召回逻辑、记忆规则和评估集。

### 4.4 为什么暂不直接依赖托管 File Search

托管文件检索适合快速原型，但回页需要精确控制：

- 单篇日记是否允许关联；
- 删除后哪些派生数据必须失效；
- 时间、来源、类型和权限过滤；
- 原文、摘要、推断之间的区别；
- 召回结果评估和解释。

因此正式版本更适合自建轻量检索管线。早期可以用托管检索做技术对照实验，但不应让它成为不可见的核心数据层。

## 5. 数据模型：原文与 AI 产物必须分层

### 日记原文

用户真正输入或导入的内容。创建后永远保留，不被 AI 静默覆盖。

### 日记版本

用户编辑、AI 整理以及用户接受后的版本。每个版本记录创建方式和时间。

### AI 整理结果

标题、标签、结构、问题与待验证内容。它是候选产物，不是事实。

### 检索片段

只从允许关联的版本生成。包含日记 ID、时间、来源和权限状态。

### 关联摘要

AI 对多篇记录形成的阶段性压缩，可查看、修正和删除，始终链接原始证据。

### 对话

用户与 AI 的真实交流记录。对话可以引用日记，但不自动写入长期记忆。

### 记忆候选

AI 从对话中提出、等待用户确认的理解。

### 已确认记忆

只有用户明确确认后保存；可修改、删除，并链接形成它的对话和证据。

核心原则：

> 文档保存真实的过去；RAG 临时找回；记忆只保存交流中经过确认的理解。

## 6. RAG 路线

RAG 不是记忆，而是“当前问题发生时，临时找回可能相关的过去”。

### 写入流程

```text
保存原文
  → 检查 AI 关联权限
  → 按自然段 / 问题 / 结论切分
  → 生成关键词索引和向量
  → 保存时间、来源、类型、权限等元数据
```

### 查询流程

```text
用户当前问题
  → 判断需要哪些时间与内容范围
  → 关键词检索（明确词语、书名、人名）
  → 向量检索（含义相近、措辞不同）
  → 时间、来源、权限过滤
  → 融合排序
  → 重排与最低相关性阈值
  → 选少量证据进入模型
  → 回答并标明引用来源
```

当前主流且适合本项目的是**混合检索**：关键词检索和向量检索并行，再融合排序。Cloudflare 当前的 AI Search 也采用向量搜索＋BM25，并支持 RRF 等融合方式；但首版可以自己用 FTS5＋Vectorize 实现简单可控版本。

### 召回评价

每次引用旁提供轻量反馈：

- 相关；
- 有点牵强；
- 完全无关；
- 有启发；
- 不希望以后关联这类内容。

这些反馈比继续调 Prompt 更重要，它们会形成项目自己的评估集。

## 7. 记忆系统路线

### 不直接记文档

日记原文本身已经存在数据库中，不需要复制成“记忆”。记忆负责保存跨文档、跨对话形成的理解。

### 不自动把推断变成事实

错误做法：

> AI 从一篇日记推断“用户是完美主义者”，并直接记住。

正确做法：

> AI 在对话中提出：“你是否经常因为不愿暴露不成熟而推迟反馈？”用户确认后，才形成可修正的记忆。

### 记忆写入流程

```text
对话结束
  → AI 提出 0–3 条记忆候选
  → 检查是否已有相似或冲突记忆
  → 用户确认 / 修改 / 忽略
  → 保存为已确认记忆
  → 未来对话按需召回
```

### 记忆必须具备

- 来源；
- 形成日期；
- 用户确认状态；
- 可修改与删除；
- 置信度或适用条件；
- 与旧记忆的冲突和变化关系。

## 8. 功能优先级

### P0：可靠的普通日记

这是进入 AI 之前必须完成的底座。

1. 写作、查看、编辑、删除和搜索稳定。
2. 自动草稿保存与异常恢复。
3. 原文和版本历史。
4. Markdown 单篇、多篇、全部导入导出。
5. 标签管理与基础筛选。
6. 图片附件管理。
7. 正式账号、跨设备同步和数据持久化。
8. 两个 AI 权限开关及批量权限管理。
9. 数据删除、账户导出和隐私说明。

完成标准：即使没有 AI，用户也愿意持续使用它记录。

### P1：真实 AI 整理

1. 标题、段落和 Markdown 格式整理。
2. OCR 文字纠错。
3. 标签和记录类型建议。
4. 提取疑问、推理、待验证内容。
5. 原文 / 整理稿对照和接受流程。
6. 记住同类记录的整理偏好。

完成标准：AI 明显省事，但不改变用户原意。

### P2：主动对话中的真实召回

1. FTS5 关键词检索。
2. Embedding 与 Vectorize 语义检索。
3. 权限、时间、来源和类型过滤。
4. 混合排序与相关性阈值。
5. 回答引用原文、日期和来源。
6. 用户对召回结果反馈。
7. 建立第一套检索评估集。

完成标准：用户主动问过去时，系统能稳定找回正确的少量记录。

### P3：未闭合问题与思考回环

1. 提取每篇日记中的问题、判断和待验证内容。
2. 新记录写入时匹配旧问题。
3. 展示“旧疑问—真实反馈—新理解”。
4. 用户确认是否真的形成回环。
5. 保存观点变化而不是静态人格标签。

完成标准：用户感到过去的记录重新获得了生命。

### P4：对话记忆

1. 对话后生成记忆候选。
2. 用户确认后保存。
3. 记忆查看、编辑、删除和冲突处理。
4. 记忆引用证据。
5. 关闭关联或删除日记后的派生数据清理。

完成标准：AI 越用越懂用户，但用户始终能看见和控制“懂了什么”。

### P5：稀缺的被动惊喜

1. 后台发现高价值思考回环。
2. 主动提示阈值和冷却时间。
3. 只发邀请，不直接进行人格判断。
4. “有启发 / 牵强 / 不要再提示”反馈。
5. 微信读书、飞书等来源接入。

完成标准：提示少而准；关闭它不会损害产品的基础价值。

## 9. 建议开发周期

以下按个人业余时间、每周约 12–18 小时估算。

### 第 0 阶段：交互 MVP（当前，继续体验 1–2 周）

- 高频使用并记录摩擦；
- 完成自动草稿、批量筛选、删除恢复和导入；
- 固化写作、编辑、导出和权限体验。

### 第 1 阶段：数据底座（2 周）

- D1 数据表和版本模型；
- R2 图片附件；
- 正式登录和用户数据隔离；
- 跨设备同步；
- 迁移当前本地数据。

### 第 2 阶段：AI 整理（1–2 周）

- Responses API 接入；
- 结构化整理结果；
- 原文对照与接受；
- 基础成本、错误和重试机制。

### 第 3 阶段：主动 RAG（2–3 周）

- 日记切分和索引；
- FTS5＋Vectorize 混合检索；
- 权限过滤、引用和反馈；
- 20–50 条真实问题的评估集。

### 第 4 阶段：思考回环（2–3 周）

- 未闭合问题提取；
- 新旧记录匹配；
- 回环确认与观点变化展示；
- 减少牵强关联。

### 第 5 阶段：记忆与主动提示（3–5 周）

- 用户确认式记忆；
- 记忆冲突与遗忘；
- 稀缺主动提示；
- 隐私删除链路和长期评估。

### 里程碑

- **约 4 周**：有账号、跨设备、真实 AI 整理的私人 Alpha。
- **约 8–12 周**：主动召回可靠、可以给少量用户试用的 Beta。
- **约 12–18 周**：拥有思考回环、确认式记忆和克制主动提示的作品集版本。

## 10. 学习顺序

你不需要先成为全栈工程师。按产品链路理解技术：

1. 数据模型：一篇日记、一个版本、一个权限分别是什么。
2. API：前端如何安全地请求服务端保存或处理内容。
3. 数据库：怎样查询、更新、删除，并保证用户隔离。
4. Prompt 与结构化输出：如何让 AI 返回稳定、可检查的整理结果。
5. Embedding：怎样把“意思相近”变成可以计算的距离。
6. RAG：怎样从大量记录中找到正确的少量证据。
7. Evals：怎样证明召回变好了，而不只是凭感觉。
8. 记忆：什么值得长期保存，怎样确认、冲突和遗忘。

每学一层就实现一个可体验功能。不要在没有真实数据和问题之前学习复杂 Agent 框架、知识图谱或微调。

## 11. 暂时不要做

- 不要先做多 Agent 系统；
- 不要先做复杂知识图谱；
- 不要先微调模型；
- 不要先接很多外部平台；
- 不要让 AI 自动生成未经确认的人格画像；
- 不要在主动召回不准时开发被动提醒；
- 不要为了技术“高级”而重写已经可用的前端。

## 12. 作品集表达

这个项目最有价值的叙事不是“我调用了大模型”，而是：

1. 从真实记录行为中发现“思考无法继续”的问题；
2. 提出“轻量，先有用，后回味”的价值原则；
3. 将 AI 权限拆成“整理”和“关联”两个独立开关；
4. 通过高频体验持续修正写作画布；
5. 设计原文、RAG、关联摘要和确认式记忆的边界；
6. 用评估数据逐步把惊喜从演示变成可靠能力。

这同时展示产品判断、交互设计、AI 工程、隐私意识和长期迭代能力。

## 13. 当前唯一下一步

继续使用当前交互 MVP 1–2 周，记录每一次摩擦。在进入真实 AI 前，优先补齐：

1. 自动草稿保存；
2. Markdown 导入；
3. 删除后的回收站；
4. 日记筛选和批量操作；
5. 正式数据模型与云端持久化方案。

完成这些后，再进入 P1“真实 AI 整理”。

